# Aptly server configuration
#
server {
        listen 80 default_server;
        listen [::]:80 default_server;
        root /var/www/public;
        server_name _;

        location / {
                autoindex on;
        }
}
