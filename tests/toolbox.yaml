apiVersion: v1
kind: Pod
metadata:
  name: toolbox2
spec:
  containers:
  - name: toolbox
    image: horakmar/toolbox:latest
    volumeMounts:
      - mountPath: /etc/ssl/kubernetes
        name: ssl
        readOnly: true
  volumes:
    - name: ssl
      hostPath:
        path: /etc/ssl/kubernetes
#  nodeSelector:
#    role: proxy
