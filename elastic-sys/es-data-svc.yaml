apiVersion: v1
kind: Service
metadata:
  name: es-trans
  labels:
    component: elasticsearch
    role: client-data
spec:
  selector:
    component: elasticsearch
    role: client-data
  ports:
  - port: 9300
    name: transport
  clusterIP: None

---

apiVersion: v1
kind: Service
metadata:
  name: es-http
  labels:
    component: elasticsearch
    role: client-data
spec:
  selector:
    component: elasticsearch
    role: client-data
  ports:
  - name: http
    port: 80
    targetPort: 9200
